# Environment Variables for Chatbot Configuration
# Copy this to .env.local and add your API keys
# Only one provider is needed - the chatbot will automatically use the configured one

# Option 1: OpenAI (Recommended for best quality)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Option 2: Groq (Recommended for speed and cost-effectiveness)
# Get your API key from: https://console.groq.com/keys
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Option 3: Ollama (Free, runs locally)
# Make sure Ollama is running locally with: ollama serve
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# Priority order: OpenAI -> Groq -> Ollama
# The system will use the first available configuration
